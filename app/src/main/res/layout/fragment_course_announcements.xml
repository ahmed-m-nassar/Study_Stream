<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/CourseAnnouncements_Parent_Layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">
    <Button
        android:id="@+id/CourseAnnouncements_AddAnnouncement_Button"
        style="@style/ListViewButton"
        android:text="Add a new announcement" />
    <ListView
        android:id="@+id/CourseAnnouncements_AnnouncementsList_ListView"
        style="@style/ListViewStyle"
        android:layout_margin="15dp">

    </ListView>

</LinearLayout>